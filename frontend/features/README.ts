/**
 * Feature modules live here.
 *
 * Each feature is a self-contained vertical slice that may pull from
 * the shared `components/`, `services/`, `hooks/`, and `lib/`
 * directories. Examples of future features: business-profile,
 * simulation, decision-engine, reports.
 *
 * This file is a marker so the directory is preserved in version
 * control. Add a `index.ts` per feature when the first module lands.
 */
export {};
