/**
 * Reusable React hooks live here. Add domain-specific hooks (e.g.
 * `useBusinessProfile`, `useSimulation`) as features are built. This
 * file exists to keep the `hooks/` directory tracked in version
 * control.
 */
export {};
