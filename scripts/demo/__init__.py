"""Synthetic demo data scripts.

This package contains:

  - ``seed_demo_business.py`` — idempotent seed for Acme Textiles.
  - ``reset_demo_business.py`` — companion reset, the only path
    that drops demo rows.

Both scripts are documented inline and report their behavior
through the ``[DEMO-SYNTHETIC]`` log prefix.
"""
